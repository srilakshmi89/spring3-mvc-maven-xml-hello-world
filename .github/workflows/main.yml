name: java CI with maven
on:

  push:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: mvn package
    - name: tomcat deploy
      run: curl -v -u admin:admin -T /home/runner/work/spring3-mvc-maven-xml-hello-world/spring3-mvc-maven-xml-hello-world/target/spring3-mvc-maven-xml-hello-world-1.0-SNAPSHOT.war
           'http://ec2-13-233-25-173.ap-south-1.compute.amazonaws.com:8081/manager/text/deploy?path=/github_action_spring3&update=true'
